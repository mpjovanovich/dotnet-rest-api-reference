# TODO

Add some images and url property to bird model to make this more interesting and add more failure modes? This could help with later projects that extend this for other application concerns.

## After SQLite branch

Update BirdsEndpointTests to use the in memory repository for the tests - not the SQLite repository.
